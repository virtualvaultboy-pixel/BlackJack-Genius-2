name: Build Android APK (Debug)

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'service-worker.js'
      - 'privacy.html'
      - 'manifest.json'
      - 'capacitor.config.json'
      - 'package.json'
      - '.github/workflows/build-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Prepare web assets
        run: |
          mkdir -p www
          cp index.html www/
          [ -f service-worker.js ] && cp service-worker.js www/ || echo "no service-worker.js"
          [ -f privacy.html ] && cp privacy.html www/ || echo "no privacy.html"
          [ -f manifest.json ] && cp manifest.json www/ || echo "no manifest.json"
          shopt -s nullglob
          for f in icon*.png favicon*.png favicon*.ico *.svg apple-touch-icon*.png; do
            cp "$f" www/ 2>/dev/null && echo "copied $f"
          done
          echo "=== Contents of www/ ==="
          ls -la www/

      - name: Install Capacitor
        run: npm install --no-fund --no-audit

      - name: Add Android platform
        run: |
          if [ ! -d "android" ]; then
            echo "Adding Android platform"
            npx cap add android
          else
            echo "Syncing existing Android platform"
            npx cap sync android
          fi

      - name: Patch AndroidManifest.xml (permissions + portrait lock)
        run: |
          python3 << 'PYEOF'
          import os, re
          path = "android/app/src/main/AndroidManifest.xml"
          with open(path, "r", encoding="utf-8") as f:
              content = f.read()
          # 1) Permissions camera + micro
          new_perms = (
              '    <uses-permission android:name="android.permission.CAMERA" />\n'
              '    <uses-permission android:name="android.permission.RECORD_AUDIO" />\n'
              '    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />\n'
              '    <uses-feature android:name="android.hardware.camera" android:required="false" />\n'
              '    <uses-feature android:name="android.hardware.microphone" android:required="false" />\n'
          )
          if "android.permission.CAMERA" not in content:
              content = content.replace("</manifest>", new_perms + "</manifest>")
              print("Permissions injected")
          else:
              print("Permissions already present")
          # 2) v1.14 - Force orientation portrait au niveau natif Android.
          # Ajoute android:screenOrientation="portrait" sur la balise <activity>
          # si pas deja present. Bloque la rotation cote APK.
          if 'android:screenOrientation' not in content:
              content = re.sub(
                  r'(<activity\b)',
                  r'\1 android:screenOrientation="portrait"',
                  content,
                  count=1
              )
              print("Portrait orientation lock injected")
          else:
              print("screenOrientation already present")
          with open(path, "w", encoding="utf-8") as f:
              f.write(content)
          print("=== Final AndroidManifest.xml ===")
          print(content)
          PYEOF

      - name: Build Debug APK
        working-directory: android
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: bjgenius-debug-apk-${{ github.run_number }}-${{ github.run_attempt }}
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
          if-no-files-found: error
          overwrite: true
